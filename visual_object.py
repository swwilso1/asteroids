#! /usr/bin/env python

class VisualObject(object):
    """Base class for all visible, renderable objects in the game."""

    
    def draw(self, surface, point):
        """draw(surface,point) will draw the object onto surface
        starting at point where point is the upper leftmost corner of the
        object."""

        return

    # End draw

# End VisualObject
